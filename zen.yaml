commands:
  - name: server
    type: api
  - name: websocket
    type: websocket
  - name: crawler
    type: worker

moduleName: test-zen
env:
  - name: JWT_KEY
    structName: JWTKey
    type: string
  - name: PORT
    type: int
  - name: DATABASE_URL
    structName: DatabaseURL
    type: string
database:
  - name: postgres
    type: postgres
    models:
      - name: admin
        properties:
          - name: username
            type: string
          - name: password
            type: string
            secured: true

  - name: mongo
    type: mongo
    schema:
      - name: admin
        properties:
          - name: username
            type: string
          - name: password
            type: string
            secured: true

services:
  - name: admin
    database: postgres
